[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kvspace-c"
version = "0.1.0"
description = "Python ctypes wrapper for libkvspace-c.so"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
